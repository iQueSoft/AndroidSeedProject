<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="net.iquesoft.project.seed.presentation.view.activity.SplashActivity">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="843dp"
        android:layout_height="395dp"
        android:layout_above="@+id/progress_bar_splash"
        android:layout_marginLeft="@dimen/splash_image_margin"
        android:layout_marginRight="@dimen/splash_image_margin"
        android:layout_marginTop="@dimen/splash_image_margin"
        android:background="@drawable/ic_custom_launcher"
        android:contentDescription="@string/splash_image" />

    <com.trncic.library.DottedProgressBar
        android:id="@+id/progress_bar_splash"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_alignEnd="@+id/imageView"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/imageView"
        android:layout_gravity="end|bottom"
        android:padding="30dp"
        android:visibility="visible"
        app:activeDot="@color/green_from_logo"
        app:dotSize="18dp"
        app:inactiveDot="@color/white"
        app:jumpingSpeed="170"
        app:spacing="5dp" />
</RelativeLayout>
